#include <disasm.h>

#include <fstream>
#include <ios>
#include <iostream>

namespace {
}

namespace disasm {
    const char* load_binary(const char* path);
    const char* disassemble(const char* code);
}

